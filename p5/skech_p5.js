/*
* Fichier pour la gestion du canvas
* */

let h1, canvas, width = 1000, height = 500;

function setup() {
    canvas = createCanvas(width, height);
    canvas.position(500, 200, 'fixed');
}
function draw(){

}